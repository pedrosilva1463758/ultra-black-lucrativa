import AdminPanel from "@/components/AdminPanel";

export const metadata = { title: "Painel · Check-in Ultra Black", robots: { index: false, follow: false } };

export default function Admin() {
  return <AdminPanel />;
}
